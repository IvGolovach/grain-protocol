# Stabilization Corpus

This directory stores deterministic minimized regression inputs discovered during RC stabilization.

Rules:
- Only minimized, reviewable repro inputs are tracked.
- Large or exploratory corpora stay in CI artifacts.
- Every tracked corpus input must map to a documented finding and deterministic guard.
